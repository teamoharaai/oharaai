# Ohara Codex Changelog

## [Unreleased]

### Added — Goals V2.3.1 Goal controls and What's New lifecycle
- Added an extensible feature-patch data contract and deterministic seven-day selector that displays only the newest active release per independent feature category. Seen state is persisted per patch ID so later Goals, Vault, Roots, Home, Momentum, Circles, Projects, or future-feature releases remain independently eligible (`config/internal-release.ts`, `features/auth/internal-release.ts`, release modal/tests).

### Changed — Goals V2.3.1 Goal controls and What's New lifecycle
- Separated Goal identity from a focused Time Remaining card, removed duplicate category metadata and the redundant Goal overflow menu, and made Vault/Overview a primary sibling action beside Manage Goal. The shared `VaultIcon` now defines the approved layered mark for current and future Vault surfaces (`GoalDetailHeader.tsx`, `GoalVault.tsx`, shared UI controls).
- Reorganized Manage Goal into compact Details and Status & Access tabs. Existing description/category/project/deadline actions and canonical lifecycle, archive, visibility, and Circles-audience controls now live in one management surface without changing their persistence paths (`ManageGoalControl.tsx`).

### Added — Goals V2.3 Vault workspace foundation
- Added migration 070 with durable, immutable `vault_items.content_kind` classification. Only migration-062 rows with complete `goal_notes` provenance are backfilled as `sticky_note`; ambiguous and generic notes remain `generic`. New Sticky Notes are classified explicitly, while photo/content edits cannot erase identity (`supabase/migrations/070_vault_item_content_kind.sql`, Vault API/types/services).
- Added a parent-neutral `VaultWorkspace` presentation contract, functional All/Sticky Notes/Notes/Reflections/Sources filters, canonical Echo links, existing source handling, truthful Vault-only activity, section-level errors, and a compact Add to Vault menu. Project-owned schema remains deliberately deferred; the reuse and future Study Card seam are documented in `docs/goals/vault-v23-foundation.md`.
- Replaced the Overview/Vault center tabs with a Goal-header Vault/Overview action and URL-backed `view=vault` workspace state. Vault mode retains the Goal and left navigation, removes the analytics rail, and expands across the available workspace on desktop and responsive layouts.
- Added isolated migration/owner-isolation rehearsal coverage and updated light/dark responsive browser acceptance for workspace expansion, deep-link refresh, filters, canonical add/edit flows, error recovery, and zero horizontal overflow (`scripts/test-vault-v23-security.sh`, `features/goals/vault-classification.test.ts`, `tests/goals/workspace.spec.ts`).

### Changed — Goals V2.2 validated release
- Final V2.2 state supersedes the incremental pending-validation notes below: upstream `5a8887b` and migrations 001–066 are preserved; the legitimate new migrations are 067 (immutable scoring profile), 068 (four canonical categories and preserved Entry-link audit), and 069 (canonical visibility management).
- Validation passes: 377 affected unit checks, six light/dark responsive browser scenarios, isolated database/security suites, clean 001–069 reset, data-bearing 066→069 upgrade, authenticated local Task/Momentum/New Phase integration, production web export, and diff checks. TypeScript reports only the three unchanged protected duplicate-editor diagnostics. Rollback-only production rehearsal preserves all 55 Goals and complete Momentum snapshot/difficulty history, yielding 19 Health & Fitness, 1 Work & Money, 27 Learning & Creativity, and 8 Life & Relationships Goals.
- Release sequencing preserves score continuity: expand the scoring column with 067 before deploying its reader, verify the new deployed SHA, then convert taxonomy/apply visibility with 068–069. No historical test Goals are removed. Historical Roots reference assets were restored solely for local baseline validation and are not part of the product changes.

### Added
- Added a rollback-only production migration rehearsal with a 066 ledger guard, short lock timeout, approved category/profile checks, and full logical Goal/Momentum history comparisons; it never commits or changes the migration ledger.
- Preserved approved legacy Goal reads during the staged rollout by passing Goal IDs through Goal/Entry category adapters; unknown `mind` values still fail closed. New writes remain canonical-only. Scoring-profile expansion must precede deployment, and taxonomy conversion must follow deployment of profile-aware Momentum reads.
- Expanded manual creation browser coverage to commit explicit Task/Milestone drafts only on final creation, while the skip path clears both without persistence.
- Added localhost-only signed-in API/Momentum integration coverage and actual New Phase RPC coverage for an approved Health & Fitness Goal retaining its legacy Education profile. Synthetic fixtures remain in the disposable local stack because existing immutable snapshot triggers intentionally prevent cascading deletion; no triggers are bypassed and no production test records are deleted.
- Added an explicit accessible heading to the manual creation page and waited for modal dismissal before visual acceptance captures.
- Added the durable Goal scoring-profile field to the Supabase Goal row/insert/update types; database triggers remain authoritative for defaults and immutability.
- Preserved loaded private Sticky Notes/folders when Manage Goal refreshes core metadata, matching upstream’s separate Vault hydration boundary rather than clearing the private workspace cache.
- Updated the agent-session default to exact `Learning & Creativity`, avoiding a rejected legacy `creative` fallback after the canonical category guard changes.
- Extended workspace browser acceptance to Manage Goal audience-read failure/retry, disabled unsafe saving, distinct lifecycle controls, and canonical visibility RPC payloads. Updated the Task architecture assertion to follow the extracted V2.2 manual component while still requiring canonical Task persistence/composer reuse.
- Preserved the optional title-example interaction with explicit “Use this”/“Keep mine” controls and accurate non-AI copy; never replaces typed intent automatically. Unified Goal/Momentum presentation with the approved product adapter instead of silently reclassifying legacy `mind` values.
- Documented durable product-category/scoring-profile separation and added full V1.1 difficulty/score equality tests across historical profiles, all ten approved `mind` mappings, and later category edits. Added deterministic draft-suggestion context coverage (`docs/goals-v22-scoring-continuity.md`, Momentum engine tests, Goal suggestion tests).
- Reconciled upstream `5a8887b`: preserved Task reconciliation optimizations, owner-private Sticky Note folders, and Recent Activity in the analytics rail. Read-only production ledger confirms 063–066 are already applied; renumbered only our unapplied V2.2 migrations to 067–069. Earlier local validation must be repeated on this combined base. Restored exact historical Roots reference assets required by its existing hash/dimension tests, without changing Roots behavior.
- Updated lifecycle and workspace regression fixtures to canonical categories and upstream Vault note-items; verified weekly-count/quantity behavior, private/public New Phase, rollback, mixed schedules, profile inheritance, and Vault viewer denial against the disposable full schema. Reset all manual draft defaults on “Create another” and kept navigation from shrinking on narrow screens.
- Hardened visibility loading/retry so a failed audience read cannot accidentally save a private default; added explicit accessible category/visibility selection states and canonical draft Task save semantics during browser validation.
- Added draft/suggestion and light/dark desktop/narrow manual-flow browser coverage, plus full-schema visibility transition tests for accepted-invite access, revocation, cross-owner denial, and private Vault exclusion. Clean local migrations 001–065 have applied successfully; production is unchanged.
- Added the four-step manual V2.2 surface with exact category values, retained rhythm, empty optional Plan, explicit draft-only contextual suggestions, existing Task/Milestone composers, shared visibility control, and resumable setup after partial-save errors. Runtime/browser/security acceptance is not yet complete.
- Added owner Manage Goal UI using canonical completion/archive callbacks and the existing `stagnant` Pause state, with shared visibility editing and category/title editing. Exposed existing Task and Milestone composers for draft reuse without replacing their behavior.
- Added a shared Goal visibility control/service and pending owner-authorized transition RPC (065), retaining accepted-friend invitations, curated public access, revocation history, and private Vault boundaries. No broader public/profile access or RLS expansion was introduced; security validation remains pending.
- Added pending Migration 064 with approved per-Goal product mappings, strict unknown-value rejection, canonical Entry category consolidation with owner-private original-link audit records, and updated session/Entry relationship RPC category guards. Historical Circles snapshots and Momentum tables are not rewritten.
- Added a production-independent disposable PostgreSQL harness covering scoring-profile backfill/defaults, category-edit immutability, successor inheritance, unchanged timestamps, and untouched snapshot data (`scripts/test-goal-scoring-profile.sh`, `tests/goals/scoring-profile.test.sql`).
- Switched primary creation category options to the four exact product values and reused existing color/icon tokens for canonical Goal/Roots presentation. Historical template aliases remain compatibility-only; complete consumer/migration validation is still pending.
- Updated Momentum source reads, event categorization, difficulty inputs, and plan revision inputs to use the established profile, not editable product category. Legacy-only fixture fallback fails closed for missing migrated profiles (`features/momentum/services/momentum-service.ts`, `scoring-profile.ts`).
- Added pending Migration 063 for immutable Goal-level `momentum_scoring_profile`, preserving pre-migration profiles and enforcing same-owner New Phase inheritance without changing snapshots or Momentum V1.1 constants. Added adapter regression tests; database and end-to-end release validation remain pending.
- Started Goals V2.2 taxonomy/scoring separation: added the exact four product categories, approved per-Goal legacy `mind` mappings, fail-closed legacy conversion, and the approved new-Goal Momentum compatibility defaults (`lib/goals/product-categories.ts`, `features/momentum/scoring-profile.ts`). This foundation does not yet change production data, existing scoring formulas, or application consumers; release validation remains pending.

### Fixed — New Phase cadence and public visibility
- Added Migration 060 to replace the deployed New Phase RPC without rewriting applied migrations: copied schedules retain `target_count`, and a public predecessor relinquishes its slot inside the same owner-locked transaction before the active public successor is inserted. Private visibility, lineage, predecessor history, Task eligibility, and new independent occurrence generation remain unchanged. No Task UI or Momentum formula changes.
- Added private/public New Phase, mixed once/daily/weekday/weekly-count/quantity definitions, valid successor Milestone links, predecessor history/private-note preservation, cross-user denial, duplicate phase rejection, and injected-failure rollback assertions (`tests/goals/*phase*.test.sql`, `release-db.test.sql`).
- Validation: clean 001–060 and production-shaped 059→060 paths yield the same New Phase function hash and pass all three release SQL matrices. Unit suites, existing isolated database-security suites, and both light/dark responsive browser scenarios pass. Production read-only dry run lists only 060; no applied migration is replayed. Supersedes the earlier recorded New Phase blockers without changing the approved Task internals or Overview/Vault composition.

### Changed — Final upstream Task reconciliation
- Release gate remains BLOCKED before commit/push. Clean 001–059 and fixture-backed 054→059 upgrades pass, but added lifecycle acceptance reproduces loss of `target_count` on New Phase; a public predecessor also fails `goals_one_public_per_user`. Read-only hashes confirm the relevant local and production RPCs/index are identical. No migration, production write, ledger repair, or implicit public-sharing transfer was introduced. The failing lifecycle regression is retained for the follow-up fix.
- Added a transactional full-schema database acceptance script for weekly-count create/edit/target persistence, idempotent quantity steps and undo, owner Sticky Note editing, cross-user denial, private photo storage, and exclusion from shared/public/Project RPCs (`tests/goals/release-db.test.sql`). All 59 production ledger filenames match the repository; no pending migration or ledger repair is needed.
- Corrected weekly-count occurrence projection so current-week targets remain in Today’s Focus after their anchor day; normalized Momentum’s due expectation at the ISO period end, not Monday, retaining one quantity commitment and all V1.1 formulas/weights. Added current/partial-week, completed, and closed-period regression tests (`features/tasks/utils*`, `features/momentum/task-adapter*`). Removed a duplicate Milestone heading left by prior reconciliation.
- Integrated CEO commits `00c8c24` and `a04c70d`, locating Migration 059 and its canonical weekly-count types, validation, API argument, mapper, and read-only cadence editor. Preserved the upstream Task component exactly, including its always-open unified composer, reset behavior, current scope/card presentation, and optimistic handlers. Updated workspace browser selectors for that accepted composer rather than restoring an obsolete New Task button.
- Retained Overview/Vault, owner-private Sticky Notes composition, Prep retirement, and canonical Goals/Home Shared Goals. The previously missing Migration 059 source is now present; final release validation is ongoing.

### Changed — Approved upstream product reconciliation
- Post-integration validation: Tasks, Momentum, Circles, Friends, Entries, Auth, and all 23 Goal unit checks pass; both responsive light/dark browser scenarios pass, including Sticky Note create/edit inside Vault. TypeScript reports only the three pre-existing errors in protected untracked `RichTextEditor.web 2.tsx`; diff whitespace checks pass. Production ledger includes 055–059 and zero remaining Prep rows. Release is blocked before commit/push: live 059 adds `weekly_count`/`target_count`, absent from upstream `718c710` and unsupported by its Task validation/mapping. No production schema changes were made; full production acceptance remains pending reconciliation.
- Replaced a stale Milestone zone-label reference left by reconciliation with the existing shared section heading, preventing the Goal workspace from failing to render (`MilestonesPanel.tsx`).
- Integrated upstream through `718c710`, including Task caching, auth timeout fixes, deadline-density pickers, post-055 Milestone behavior, counter/Reminder/To-do flows, and private Sticky Notes persistence. Prep retirement takes precedence: no Prep controls or data restoration are included.
- Reused upstream `StickyNotesPanel` and its owner CRUD/photo callbacks inside Vault's Private Notes section, preceding linked Echo material, recent activity, and completion history. The workspace retains exactly Overview/Vault. Sticky Notes remain separate from Echo, shared/public Goal DTOs, and Project aggregation. Preserved V2.1 sibling cards, canonical Shared Goals and existing Home behavior.

### Added — Goals V2.1 — Workspace Polish & Shared Goals Integration
- Connected the compact Goals Shared Goals card to Home's canonical Circles store/service/RPC and existing view-only SharedGoalSheet, preserving RPC response order (latest accepted invitation first), a three-row preview, and a bounded full-list modal. No private owner Goal route, mutation controls, or new sharing schema was added (`SharedGoalsPreview.tsx`, `GoalsWorkspace.tsx`).
- Added deduplicated shared-source loading/error/retry state used by both Home initialization and Goals, with Circles cache clearing on sign-out and session guards for pending initial requests (`features/circles/store.ts`, `store/clearAllStores.ts`).
- Reconciled 22 upstream commits through `596389b`: retained live Circles, Home SWR aggregation, Echo fixes, account-menu separation, top-level reminder selection, editorial navigation typography, and deadline popover/calendar behavior. Kept the V2.1 countdown/header placement, separate planning cards, Vault composition, and analytics polish. Superseded the old Friends-to-Circles label tweak with upstream's distinct People/Circles surfaces.
- Updated the stale Momentum refresh architecture assertion to follow current canonical Goal/Task mutation handlers after upstream removed Home check-off handlers; no Momentum implementation changed. Verified shared viewer/metadata consistency between Home and Goals, request reuse, three-row cap, retry/empty states, and responsive light/dark composition with isolated browser fixtures.

### Changed — Final Overview and analytics composition
- Split Overview into sibling Task and Milestone floating surfaces with matching responsive padding; retained next-step navigation and all existing mutation callbacks (`GoalsWorkspace.tsx`).
- Replaced Prep/Milestone creation triggers with the same compact primary Button as Add Task, retaining existing Prep submission and Milestone editor/Path/photo flows; removed dashed empty-state creation treatments (`MilestonesPanel.tsx`).
- Moved Week/Month Activity above Momentum Trend and increased the chart to 287px total height / 240px plot height, preserving all data, calculations, axes, and gradient treatments (`GoalsWorkspace.tsx`). Goal header and Vault architecture remain untouched.
- Validated 69 Task/activity tests, 70 Momentum tests, 11 Goal workspace/lifecycle tests, and two light/dark browser scenarios at 1440/768/390px including Prep disclosure and Milestone editor open/cancel. Web export and diff checks pass; TypeScript retains only the three pre-existing duplicate-editor errors. No dedicated Milestone Paths test suite exists; Path/photo handlers were left unchanged.

### Changed — Goals V2.1 hierarchy polish
- Restored the thin elapsed-time gradient beneath the countdown, without restoring the Goal completion ring. Standardized Tasks/Milestones/Vault major section headings with `GoalSectionHeading`, retained muted micro-labels, and removed competing full-width planning/tab dividers.
- Moved the unchanged Week/Month activity calendar beside Momentum under Goal Analytics. Split `GoalVault` into linked Notes/Reflections and existing material, concise recent activity, and a strictly Task/Milestone completion timeline; added actionable partial-source retry without changing backend data or calculations. Updated responsive browser fixtures and assertions.
- Audited card radius, inset dividers, header padding, and mobile section alignment; normalized header spacing to site tokens and reduced Next Step gaps. Validation: Task/activity (69), Momentum (70), Goal UI/lifecycle (11), and light/dark browser scenarios pass, including partial Vault failure/retry. Web export succeeds; TypeScript still reports only the three existing duplicate-editor errors. Screenshots are emitted to `/tmp/ohara-goals-v21-*` by the browser suite.

### Changed
- Reorganized Goals V2.1 into a compact Recent Goals/Shared Goals rail, a countdown-led header, Overview/Vault tabs, one Task-and-Milestone planning surface, and contextual Intelligence/Momentum cards. Vault presents existing linked Echo entries, stored material, and progressively disclosed activity without changing storage or Task/Milestone/lifecycle behavior (Goals workspace, header, and new GoalVault component).
- Restored desktop navigation icons, labeled the existing Constellation destination Roots, and increased usable Goal Momentum chart height with readable endpoint dates; formulas, categories, and source data remain unchanged.
- Labeled the existing social popover Circles, recorded session-local Goal interaction recency (cleared on sign-out), and retained next-step navigation within the unified planning panel using the page shell's native scroll target. Added isolated light/dark desktop, tablet, and mobile browser coverage in `tests/goals/`; Task, Momentum, Friends, and Goal regression suites pass. Production web export passes; repository typecheck remains blocked only by three pre-existing errors in the untouched `RichTextEditor.web 2.tsx` duplicate.

### Fixed
- Prevented incomplete milestone evidence children from being selected as a Goal Overview “Next Step”; the feature now uses top-level milestones only (`features/goals/goals-workspace.ts` and focused selector tests).
- Fixed the Echo Reflections control reverting to Notes by removing the library-local reset effect and making `view=reflection` the stable source of truth. Reflection compatibility/deep-link routes now carry that view through the unified Entries workspace.
- Reset the Add/Edit Task draft whenever its modal closes so reopening Add Task cannot retain prior values, and seed retroactive completion input from the user's local wall clock instead of UTC so valid current times are not rejected as future times in negative UTC offsets (`features/tasks/components/TasksPanel.tsx`, `features/tasks/utils.ts`, `features/tasks/utils.test.ts`, and `features/tasks/architecture.test.ts`).
- Updated the server-side authentication redirect fallback to the Vercel-attached public production domain, `https://www.oharaai.com`, because the historical `oharaai.vercel.app` alias is no longer attached to the project (`lib/auth/redirects.ts`).

### Added
- Added the **Home load aggregator** — `GET /api/home/summary` (`app/api/home/summary+api.ts`, `lib/db/home-summary.ts`) returns Today's Focus's goal-derived signals (this week's canonical `task_occurrences` Task counts + reflection-ordering timestamps) in one server round-trip computed in parallel, scoped to the caller (it resolves its own active Goals), behind the SWR-cached `useHomeSummary` hook (`features/goals/hooks/useHomeSummary.ts` + `features/goals/services/home-summary-service.ts`). A new Home signal is a field on this aggregator, not a new client round-trip.
- Added a dedicated Home “Next Step” panel beneath Today’s Focus. It shows up to three dated, upcoming top-level milestones across active Goals, opens the owning Goal, labels imminent and overdue dates, and explains how to populate the empty state (`app/(app)/dashboard.tsx`, `features/goals/goals-workspace.ts`, and focused selector tests).
- Shipped **Circles — Phase 8 go-live (initiative session 008).** Flipped `FEATURES.CIRCLES_ENABLED` from `false` → **`true`** in `constants/features.ts` (Home now renders the live friends-only feed for all users; migrations 053 + 054 already live) and, on the user's explicit go-ahead, merged `feat/circles-home` into **`main` directly** (fast-forward; no PR) and pushed. The user signed off the Phase 8 signed-in QA rather than running it step-by-step; the deferred browser checks (profile-popover position CD-010, light/dark, <720 / 720–1080 / ≥1080 width breakpoints, mobile avatar menu, and the Phase 5a Today's-Focus weekly-count render) become post-ship watch items. Committed only the Circles files — the unrelated in-flight Echo/Entries working-tree edits (`features/entries/*`, `docs/ECHO_PIPELINE.md`, `EchoNewMenu.tsx`) and the untracked `.claude/launch.json` were deliberately left out. `npx tsc --noEmit` clean; `npm run test:circles` 28/28; `npm run test:circles:db` green. A team handoff brief was generated at `design/circles/Circles-Feature-Brief.pdf` (untracked). Phase 5b (invite links) stays deferred (CD-022). See `design/circles/OUTSTANDING.md`.
- Added **Circles — Phase 7: docs (initiative session 007).** Docs-only; brought the written record in line with a shipped Circles layer. `docs/CLAUDE.md` (the root constitution — repo-root `CLAUDE.md` is a symlink to it): **Data Model** gained a **Circles** bullet (friends-only layer = Home `/dashboard`; the 053–054 tables + `goals_one_public_per_user` index; the CD-004 privacy spine via the whitelisted `circles_goal_summary`; the `FEATURES.CIRCLES_ENABLED` gate; server `lib/db/circles.ts` + `lib/db/circles-core.ts` + `app/api/circles/**`, client `features/circles/`); **What NOT To Build** lifted the blanket "No feed" (Circles/Home is now named as the one shipped social surface behind the flag, while a public/discovery feed, profile pages, and social push notifications stay Phase-2 "not built"); **Naming** added Circles (= Home). `docs/API_CONTRACT.md` documented `GET /api/goals/weekly-task-counts` after the existing Circles endpoints section, **explicitly flagged as the goals lane, not `/api/circles/**`** (owner-tz Monday-start week CD-003, own goals only via RLS, goals without occurrences absent for graceful degradation, raw `{ counts: { [goalId]: { done, target } } }` response — no `ApiResponse` envelope). `docs/DECISIONS.md` (append-only) gained a dated pointer to `design/circles/DECISIONS.md` as the authoritative CD-001…CD-022 log, framing Circles as the first feature to consume a friend edge as an authorization primitive. No source/schema/test change; `npx tsc --noEmit` clean before and after. On `feat/circles-home`; not committed. Next: Phase 8 — signed-in browser QA (popover position CD-010, light/dark, width breakpoints, mobile avatar menu, Phase 5a weekly-count render) + PR from `feat/circles-home` and flip `FEATURES.CIRCLES_ENABLED` to true (no merge without user go-ahead). See `design/circles/changelog/007-phase7-docs.md`.
- Added **Circles — Phase 6: automated tests (initiative session 006).** Tests-only, no product/schema/DB behavior change. New `lib/db/circles-core.test.ts` (22 tests) covers the pure server core with relative runtime imports (D-004; the module's `@/types/supabase` import is type-only, stripped by `--experimental-strip-types`): `classifyPgError` SQLSTATE table (42501/P0002/22023/23505 + unknown→null), `mapAuthor`/`buildAuthorMap` (snake→camel, null-safe name/avatar), `mapFeedRow`/`mapPostCore` (count/flag coercion, author hydration present + null-when-unfriended, link snapshot only when kind+ref+title all present), `mapGoalSummary` (CD-004 whitelist — milestones title+done only, private fields dropped, `weeklyTask` present vs null-when-SQL-emits-null, access default), `mapSentInvite` (CD-014/CD-018 pending/declined→pending, accepted→accepted, withdrawn/unexpected→null), and every validator (`validateUuid`, post/comment body bounds, `parseCreatePostInput` link pairing + ≤280 description CD-005, `parseInviteeIds` non-empty/≤50/dedupe, `validateInviteResponse`, `parsePublicGoalId`). New `features/circles/progress.test.ts` (6 tests) covers CD-003: `milestoneProgress` (done/total, ratio null-not-0% when empty), `milestoneLabel` ("X of Y milestones" / "No milestones yet"), `weeklyTaskLabel` (`done / target this week`, null at target 0 — graceful degradation). New `"test:circles"` npm script runs both under the strip-types runner (28/28 green). Extended `scripts/circles-security.test.sql` with a `get_circles_feed` pagination section: seeds 55 stamped author posts via direct superuser inserts (053 grants `authenticated` SELECT only), then as author A asserts default page = 20, `p_limit=>1000` clamps to 50, `p_limit=>0` clamps to 1 (newest row), top-3 newest-first, `p_before` returns only strictly-older rows, and `p_before`+`p_limit` returns the two newest below the cursor — all via order-independent created_at aggregates, inside the existing rolled-back / disposable-cluster flow (`npm run test:circles:db` still green, applies 053+054). No bug surfaced (the "null at target 0" rule is enforced in SQL `circles_goal_summary` + `weeklyTaskLabel`; `mapGoalSummary` faithfully passes the SQL null through — tests assert the real contract); no new DECISIONS. `npx tsc --noEmit` clean before and after. On `feat/circles-home`; not committed. Next: Phase 7 (docs — lift CLAUDE.md "No feed", Data Model/Naming additions, `API_CONTRACT.md` for `GET /api/goals/weekly-task-counts`, root DECISIONS pointer) → Phase 8 (signed-in QA + PR; flip `CIRCLES_ENABLED`). See `design/circles/changelog/006-phase6-tests.md`.
- Added **Circles — Fix 0 (comment soft-delete) + Phase 5a (Today's Focus weekly Task count); Phase 5b deferred (initiative session 005).** **Fix 0 (CTO/L3, decided option A with the user):** new `supabase/migrations/054_circle_comment_soft_delete_rpc.sql` adds `delete_circle_comment(uuid)` SECURITY DEFINER (author-scoped via `auth.uid()`, idempotent, `raise P0002` when missing) with EXECUTE to `authenticated`, and **drops** the superseded `post_comments` `"Authors can soft delete own comments"` UPDATE policy + `update (deleted_at)` grant — resolving the Phase-4 QA blocker where an author's own comment soft-delete returned 403 `42501` (the `deleted_at IS NULL` SELECT policy hid the post-update row; audit 002). Mirrors `delete_circle_post`/CD-009 (**CD-021**). `lib/db/circles.ts:deletePostComment(commentId, client)` now calls the RPC (dropped the `userId` param + direct `.update()` + unused `CircleDataError` import); `app/api/circles/comments/[id]/index+api.ts` updated. **Applied + verified live** via the management API (transaction-wrapped, tracker row inserted — latest applied migration is now **054**), types regenerated from the live schema, and proven by a rolled-back `set local role authenticated` simulation as the owner (direct UPDATE → 0 rows / `deleted_at` stays null; RPC returns the id; re-delete → P0002) — the RLS/RPC-layer equivalent of the token pass's one failing check, now green (audit 003). Harness extended: `scripts/test-circles-security.sh` applies 053 **and 054**, `scripts/circles-security.test.sql` asserts the comment soft-delete round-trip; `npm run test:circles:db` green. Noted a live gotcha: all 053 tables carry table-level UPDATE+DELETE grants from a Supabase project-wide default (RLS is the gate; don't chase them in migrations). **Phase 5a (CD-003):** Today's Focus now shows this week's Task count (`done/target`, owner-tz Monday-start) beside the milestone fraction for own goals, degrading to no count when a goal has no materialized occurrences (the known occurrence-gap caveat). New `lib/db/tasks.ts:fetchWeeklyTaskCountsByGoal(db, userId, timezone)` (reuses `lib/time/zoned-calendar`, mirrors `circles_goal_summary` 053 L273–325), thin `GET /api/goals/weekly-task-counts` (CTO/goals lane, own goals RLS-scoped — deliberately **not** a `/api/circles/**` route since the dashboard is the slot owner), `features/goals/services/weekly-task-count-service.ts`, and `app/(app)/dashboard.tsx` wiring (mirrors the reflection-timestamps effect). **Phase 5b (invite links) deferred (CD-022):** live read-only checks confirmed `redeem_invite_link` (028) **auto-accepts** (inserts an `accepted` friend edge, not a pending request) and there is **no get-or-create "my invite link" RPC** — both contradict CD-016; reconciliation is a future L3 decision, so the prototype `InviteByLink()` placeholder stays. `npx tsc --noEmit` clean before and after. On `feat/circles-home`; not committed. Next: Phase 6 (tests) / Phase 7 (docs — incl. the new goals endpoint) → Phase 8 (signed-in QA + PR); optional literal HTTP token pass for Fix 0. See `design/circles/changelog/005-fix0-comment-delete-and-phase5a.md` and `design/circles/audits/003-migration-054-live-apply.md`.
- Added **Circles — Phase 4: client services + store swap behind a flag (initiative session 004).** Swapped the Circles client off the in-memory fixtures onto the live `/api/circles/**` endpoints, behind an additive `FEATURES.CIRCLES_ENABLED` (in `constants/features.ts`, **false** until Phase 8 QA) — VP-Product lane, no schema/migration/API-route change. New `features/circles/services/circles-service.ts`: pure async functions over `authedFetch` for all 20 routes, unwrapping the `ApiResponse<T>` envelope and throwing a typed `CirclesServiceError` (mirrors `friends-service.ts`), plus `fetchInviteableFriends()` consuming the shared `GET /api/friends` for the invite picker (**CD-020** — not a `features/friends` import). Rewrote `features/circles/store.ts`: state seeded by a single idempotent `ensureLoaded` (`Promise.allSettled` over feed + shared-with-me + friends' public goals + incoming/sent invites + public goal + linkable + friends), with optimistic update + revert for encourage/save/comment/accept-decline and reload-after-write for publish/send/withdraw/set-public-goal; `commentsByPost` + `loadComments`; `saved` + `loadSaved`. `types.ts` now re-exports the server DTOs (type-only) from `lib/db/circles-core.ts` as the feature's type surface; new `format.ts` (`toCategory`, `firstName`/`authorName`, `formatRelativeTime`, `goalStatusBadge`); `progress.ts` retargeted to the whitelisted milestone/weekly-task shapes. `hooks/useCircles.ts` gates all fetching on the flag (no fetch when off), derives the current-user `CirclesAuthor` (`me`) from the session with no extra request, and exposes `enabled`/`myId`/`me`. Every component swapped off fixtures onto DTOs: author identity now comes from the hydrated `author`/`owner` fields; **CD-004** removed `SharedGoal.why`; **CD-005** composer shows an editable snapshot description before posting; **CD-013** encourage count is tappable → encouragers modal; **CD-019** dropped prototype-only `following`/`toggleFollowing`; image upload UI + the `goal_complete` special view removed (no upload pipeline). With the flag **off**, Home renders greeting + Today's Focus + drafts with no feed and no Circles fetches on mount (`useGoalInviteCount` returns 0/no-fetch). `fixtures.ts` rewritten as **test-only** sample data in the new DTO shapes with **zero production importers** (grep-verified). `npx tsc --noEmit` clean before and after. **Live signed-in token pass ran 19/20 green** (audit 002: expo web :8099 → live project, owner `e4245ec3…`, user-pasted token) — all 8 client read endpoints returned the expected DTO shapes and a full write round-trip verified post create (CD-005 edited description persisted + server title snapshot + hydrated author), encourage + encouragers (CD-013), save, and delete; test data hard-deleted after and the server stopped. The **1 failure is a Migration 053 / Phase 3 defect, not Phase 4**: an author's comment soft-delete returns 403 `42501` because the `post_comments` SELECT policy (`deleted_at IS NULL`) rejects the post-update row (`circle_posts` is unaffected — it deletes via the `delete_circle_post` SECURITY DEFINER RPC, CD-009); fix is L3/CTO in a new migration (add a `delete_circle_comment` RPC or broaden the SELECT policy) — the Phase 4 client already reverts on the 403. On `feat/circles-home`; not committed. Next: fix the comment-delete defect (CTO), then Phase 5 — real-data wiring (weekly Task count on Today's Focus, invite links, `redeem_invite_link` verify). See `design/circles/changelog/004-phase-4-client-swap.md` and `design/circles/audits/002-phase4-live-verify.md`.
- Added `docs/ECHO_PIPELINE.md` and a canonical Echo/Entries section in `docs/API_CONTRACT.md` documenting the live Notes-versus-Reflections flow, owner-only data boundary, explicit Circles snapshot behavior, legacy `echo_entries` split, and the shared web/iOS create/read/autosave/delete contract.
- Added **Circles — Phase 3: server data-access + API layer (initiative session 003).** Built the server layer over the live Migration 053 surface so later phases can swap the Circles client off fixtures (CTO-lane; no schema change). New `lib/db/circles-core.ts` — a pure, node-testable core (type-only `@/` imports, stripped by the runner per D-004): camelCase DTOs + row→DTO mappers (`mapFeedRow`, `mapPostCore`, `mapGoalSummary`, `mapSentInvite`, `mapAuthor`), validators (`parseCreatePostInput`, `parseInviteeIds`, `validateUuid`, …), the typed `CircleDataError`, and `classifyPgError`/`throwCircleError` mapping Postgres SQLSTATEs 42501→FORBIDDEN, P0002→NOT_FOUND, 22023→INVALID_INPUT, 23505→CONFLICT. New `lib/db/circles.ts` calls the 053 RPCs + RLS-guarded tables via `createAuthedClient`: feed (+ author hydration through `get_profiles_by_ids`), post create/delete, encourage/unencourage + encouragers list (**CD-013**), comments list/create/soft-delete, save/unsave/list, public-goal get/set, friend-public-goals, shared-with-me, incoming + owner's sent invites (**CD-014** declined→pending via `mapSentInvite`, withdrawn omitted — new **CD-018**), send/respond/withdraw, and the composer's linkable-items picker. New `lib/api/circles.ts` (envelope + `CircleDataError`→HTTP: 400/403/404/409/500, mirroring `lib/api/friends.ts`) and 17 `withAuth`-wrapped route files under `app/api/circles/**` (userId from session only). Documented the routes in `docs/API_CONTRACT.md` ("Circles endpoints"); added `scripts/circles-api.smoke.mjs` + `npm run test:circles:api` (shaped like `momentum-api.smoke.mjs` — always checks the 401 guards, skips the authed create→delete round-trip unless a session env is set). `npx tsc --noEmit` clean before and after; `npm run test:circles:api` ran **green** against a live signed-in session (expo web :8099 → live `rrgiqemscnyaqkculnmb`; user-pasted access_token) — all reads 200, 401 guards, 400/404 negatives, and a create(201)→feed→delete(200) round-trip (post soft-deleted, live data clean). On `feat/circles-home` (committed `f3d6052`; docs follow-up separate). Next: Phase 4 — client services + store swap behind `FEATURES.CIRCLES_ENABLED`, remove `SharedGoal.why`. See `design/circles/changelog/003-phase-3-server-layer.md`.
- Added **Circles — Phase 2: Migration 053 promoted + applied live (initiative session 002).** Promoted the signed-off (CD-017) draft into the canonical tree: `git mv` `design/circles/db/053_circles_social_layer.sql` → `supabase/migrations/053_circles_social_layer.sql` and the security harness → `scripts/` (`circles-security-bootstrap.sql`, `circles-security.test.sql`, `test-circles-security.sh` — repointed to the repo-root path convention, `MIGRATION_053` now under `supabase/migrations/`); added `"test:circles:db"` to `package.json` (`npm run test:circles:db` passes). Live read-only pre-flight confirmed 0 public goals (partial unique index safe), no user with >1 public goal, `053` absent from `schema_migrations` (latest applied was `052`), and a healthy occurrence horizon (global max ~4 weeks out) — one pre-existing Tasks-materialization edge noted (active daily task `985be6df…` has no occurrences → Circles weekly count under-reports, never wrong data). With explicit user go-ahead, applied 053 to the live project `rrgiqemscnyaqkculnmb` via the management API (curl UA, transaction-wrapped), inserted the `supabase_migrations.schema_migrations` tracker row, and regenerated `types/supabase.ts` (1960 → 3512 lines; prior file was stale ~migration 034). Verified live: 5 RLS-enabled tables (`goal_share_invites`, `circle_posts`, `post_encouragements`, `post_comments`, `saved_posts`), the `goals_one_public_per_user` index, all 13 RPCs, the authored policy counts (invites/posts select-only, mutations RPC-only), and the CD-004 grant boundary — `circles_goal_summary` is NOT executable by `authenticated`. `npx tsc --noEmit` clean throughout. Latest applied migration is now **053**. `supabase/CLAUDE.md` ledger updated (052 + 053). On `feat/circles-home`; not committed. Next: Phase 3 — `lib/db/circles.ts` + `app/api/circles/**`. See `design/circles/changelog/002-phase-2-promote-apply.md` and `design/circles/audits/001-migration-053-live-verify.md`.
- Added **Circles — Phase 1b L3 sign-off (initiative session 001, docs only).** Presented draft Migration 053 (tables, RLS, RPCs, grants) for L3 sign-off and resolved open questions Q1–Q4 with the user. **053 approved as-is (CD-017)** — none of Q1–Q4 changed the schema, so the migration and local harness are unchanged: **Q1/CD-013** Encourage shows *who* (names on tap via `get_profiles_by_ids`); **Q2/CD-014** owner's sent list hides declines (maps declined→"Pending" at the API layer); **Q3/CD-015** posts linking a now-private/withdrawn Goal are kept (title snapshots stand, per CD-005); **Q4/CD-016** invite links reusable, redeem auto-sends a *pending* friend request (verify `redeem_invite_link` in Phase 5). CLAUDE.md "No feed" lift + Data Model/Naming additions deferred to Phase 7. Re-ran `design/circles/db/run-circles-security.sh` → all assertions pass (053 unchanged). `npx tsc --noEmit` clean. No product code or schema changed; still on `feat/circles-home`, nothing applied live. Next: Phase 2 — promote 053 into `supabase/migrations/`, move harness to `scripts/`, live pre-flight reads, then ask before applying. See `design/circles/changelog/001-phase-1b-signoff.md`.
- Added **Circles — prototype + Migration 053 draft (initiative session 000).** Home (`/dashboard`, still labeled Home) now renders the fixture-backed Circles social layer (`features/circles/`), keeping the greeting, Today's Focus (progress now = completed top-level milestones), the drafts list used by `AIGoalCreation`, and the Echo reconcile call. Privacy model: private by default, one optional public Goal, explicit Goal invitations accepted in Requests, feed links limited to title + description; profile panel gains Circles and Saved tabs, goal invitations in Requests, invite-by-link inside Add people, and a dropdown position fitted to the top-nav avatar. Drafted Migration 053 (`design/circles/db/053_circles_social_layer.sql`, **not yet in `supabase/migrations/`, not applied**): `are_friends`, one-public-goal index + `set_public_goal`, `goal_share_invites` + RPCs, whitelisted SECURITY DEFINER goal summaries (viewers never read base tables), `circle_posts` / `post_encouragements` / `post_comments` / `saved_posts` with RLS and post RPCs. Isolated local PostgreSQL 16 harness (`design/circles/db/run-circles-security.sh`) passes and caught a reflection-link validation bug fixed in the draft. Prototype committed on `feat/circles-home` (`f083ba5`); `npx tsc --noEmit` clean. Next: L3 sign-off. See `design/circles/`.
- Added **Tasks salvage — T4 goal-activity cross-feature union + heatmap (initiative session 005).** Extended the L1 "goal engagement" signal from occurrences-only (Phase B) to the full cross-feature UNION (Phase C: `task_completed` + `entry_created` + `milestone_completed`) and added a GitHub-style heatmap render — with the L1 output shape (`ActivityDayBucket`/`GoalActivityEvent`) FROZEN (honoring the PR #21 sign-off caveat, TD-012). `buildActivityWindow` was already union-capable and is UNCHANGED. New pure `lib/activity/goal-activity-sources.ts` holds source-agnostic row→event normalizers (`taskOccurrenceRowsToEvents`/`entryLinkRowsToEvents`/`milestoneRowsToEvents`) — the cross-feature union generalized into `lib/` so the `lib/db` reader composes it without importing `features/*` (relative-import + node-tested, D-004). `lib/db/goal-activity.ts` gained `fetchEntryCreatedEvents` (goal-linked Entries: `echo_entry_links` `container_type='goal'` + `confirmed=true` → `echo_entries.created_at`, mirroring `fetchLatestReflectionTimestamps`) and `fetchMilestoneCompletedEvents` (`milestones.completed_at`, goal+owner scoped, NULL=pending skipped); `entry_created` deliberately uses `created_at` not `updated_at` (`dashboard-goal-activity.ts`'s "latest activity" reducer left intact — **TD-014**). New pure `lib/activity/activity-heatmap.ts` (`groupBucketsIntoWeeks` Monday-aligned via `startOfIsoWeekYmd` + `heatmapIntensityLevel`, node-tested). `GoalActivityRow` now renders a multi-emblem set (one colored glyph per distinct kind); new pure `GoalActivityHeatmap` mounts in `GoalAnalyticsCard` under a new "ACTIVITY" heading beside "LAST 7 DAYS". One `useGoalActivityWindow(goal.id, 70)` call feeds both (row = last 7 buckets) — **TD-013** (window=70, single read). The route widened to `UNION_SOURCES`; `types/activity.ts` untouched (no consumer needs an `entry_created` `ActivityItem`). Pre-commit `/code-review high` applied two fixes (single-window reuse; today keeps its ring when active). `npx tsc --noEmit` clean; `npm run test:tasks` **63/63** (+16, re-verified on `main` post-merge). **Merged to `main` via PR #22 (squash `a73ffde`) on the user's explicit go-ahead — TD-005 teammate sign-off (cond. a) WAIVED, not obtained (TD-015); cloud `/code-review ultra 22` was blocked by Claude GitHub App repo access, so the pre-commit `/code-review high` stands.** Initiative #1 complete; T5 (server-side date-bounding the union reads) remains conditional. See `tracker-metrics/tasks/changelog/004-goal-activity-union-heatmap.md`.
- Added **Tasks salvage — T3 goal-activity L1 + 7-day row (initiative session 003).** Introduced the L1 "goal engagement" signal: a pure, source-agnostic, timezone-agnostic `buildActivityWindow` in `lib/activity/goal-activity.ts` that buckets normalized `GoalActivityEvent[]` (each carrying a local `YYYY-MM-DD`) into exactly `days` contiguous `ActivityDayBucket[]`, oldest→newest with today last, distinct `kinds` in declaration order, empty days as hollow cells, no `Date.now()`/`Intl` (node-tested, 9 cases). Added the `lib/db/goal-activity.ts` reader (`fetchGoalActivityEvents` + `fetchProfileTimezone`) resolving each `task_occurrences` completion to the user's `profiles.timezone` local day via `lib/time/zoned-calendar.ts` (Phase B `task_completed` source only; goal-scoped; never reads the frozen trackers/tracker_logs). Extended `types/activity.ts` (extend-only) with a `task_completed` kind + `TaskCompletedActivity`. Rendered the 7-day MTWTFSS emblem row (`features/goals/components/GoalActivityRow.tsx`, pure props consumer) inside `GoalAnalyticsCard` under "LAST 7 DAYS", fed by `useGoalActivityWindow` → new `app/api/goals/activity-window` route. Per **TD-010**, the design's `AnalyticsPanel`/`IntelligencePanel` anchors are unmounted so the row landed on the live analytics card instead. The L1 output is the stable, documented seam for the future weekly-recap pipeline (TD-007, kept OFF) and the Phase C union + heatmap (T4). `npx tsc --noEmit` clean; `npm run test:tasks` 47/47. Lands on `feat/port-tracker-optimism-boundary` via reviewed PR + teammate sign-off (TD-005) — not pushed to main. See `tracker-metrics/tasks/changelog/003-goal-activity-l1.md`.
- Added **Tasks salvage — T2 Upcoming collapse (initiative session 002).** Fixed the "Upcoming spam" regression where a daily task materialized across the 28-day occurrence horizon rendered ~27 identical Upcoming rows. `buildTaskSections` (`features/tasks/utils.ts`) now de-dupes `sections.upcoming` to one entry per task — its earliest future actionable occurrence — after the existing `occurrenceSortValue` sort, preserving cross-task next-date ordering and leaving `today`/`anytime`/`completed` and the `{ task, occurrence }` shape untouched (so `fetchTodayTaskItems` is unaffected). Added a pure `shortDate(YYYY-MM-DD)` helper (hand-parsed, no `Date`/UTC drift). `TasksPanel` gained a `TaskRow.showNextDate` prop so the Upcoming section's secondary line reads `scheduleLabel · next <shortDate>`; the `slice(0, full ? 30 : 4)` cap now caps tasks, not occurrences. Pure/UI-only — no schema/RPC/horizon change (TD-002, `design/001` Part A). Added 6 cases to `features/tasks/utils.test.ts`; `npx tsc --noEmit` clean; `npm run test:tasks` 38/38. Lands on `feat/port-tracker-optimism-boundary` via reviewed PR + teammate sign-off (TD-005) — not pushed to main. See `tracker-metrics/tasks/changelog/002-upcoming-collapse.md`.
- Added **Tasks salvage — design settlement (initiative session 001, docs only).** Resolved the audit-000 §5 open questions with the user and took `tracker-metrics/tasks/PLAN.md` off DRAFT. Reviewed Ohara Intelligence and Vaults and reframed the old "7-dot" as a goal-**activity (L1)** signal: a pure, source-agnostic derivation in `lib/` (`buildActivityWindow`) bucketing engagement per local day via `profiles.timezone` + `lib/time/zoned-calendar.ts`, rendering as a 7-day weekday emblem row (occurrences-first) then a cross-feature union + GitHub-style heatmap, and stable enough to feed the teammate's future weekly-recap pipeline (that L2 correlation seam is designed but kept OFF — no `character_profile` writes). Split the Vaults↔Notes↔Intelligence work into a separate initiative (Vault-as-corpus model; `insight` item = confirmable saved insight; direction Vaults→Intelligence). Dropped iOS reminders + the iOS-contract scope. Upcoming "spam" confirmed as one-row-per-task collapse; monthly cadence declined (daily/weekly only); `features/tasks/` changes land via design sign-off → reviewed PRs. New/updated docs under `tracker-metrics/tasks/`: `DECISIONS.md` (TD-002, TD-004…TD-009), `PLAN.md`, `design/001-activity-and-upcoming.md`, `design/002-vault-corpus-intelligence.md`, `OUTSTANDING.md`, `changelog/001-design-settlement.md`. Also corrected stale CLAUDE.md constitution text (user-authorized): `docs/CLAUDE.md` (root symlink) + `lib/ai/CLAUDE.md` now reflect **Tasks** (migrations 047–051) as canonical with legacy `trackers`/`tracker_logs` frozen/read-only, and mark `lib/ai/vault-insights.ts` as PLANNED/not-yet-built. No product code changed; `tsc`/tests not run (docs only).
- Added **Goals V2.0 — Phase 2B release cutover hardening** in Migration 051: aggregate-only mapping verification plus service-role-only catch-up, authenticated legacy-write freeze, atomic finalization, and narrow privilege restoration for an explicitly authorized application rollback. The disposable PostgreSQL harness now proves late post-049 writes are caught exactly once before legacy mutations are frozen while canonical Task RPCs remain writable.
- Added **Goals V2.0 — Phase 2B Tasks & Activity Foundation** through append-only migrations 048–050: canonical owner-scoped `tasks`, versioned IANA-timezone `task_schedules`, durable `task_occurrences`, retry receipts, trusted mutation/reconciliation RPCs, idempotent Tracker/action-log backfill, and atomic New Phase Task continuity.
- Added the canonical Task API/service/hook layer and Goal Task experience with Today, Upcoming, Anytime, completed history, binary and quantity interaction, optional Milestone links, no-deadline and deadline Tasks, daily/weekly/biweekly/custom-weekday schedules, schedule start/end/time controls, editing, archival, and retroactive completed logging in `app/api/tasks/`, `features/tasks/`, and `lib/db/tasks.ts`.
- Added production-shaped disposable PostgreSQL coverage for the 046-before-045 migration state, RLS/ownership, cross-user and inactive-Goal rejection, milestone consistency, binary undo, atomic quantity changes, real concurrent retries/reconciliation, recurrence, schedule versions, one-time revisions, midnight/DST/timezone behavior, backfill preservation/idempotency, and New Phase continuity in `scripts/tasks-*`.
- Added a temporary, non-merging legacy-versus-Task comparison API and Momentum parity diagnostic. Canonical Task occurrences now normalize into the existing Momentum V1.1 evidence contracts without changing weights, formulas, algorithm versions, provisional baselines, or closed snapshot immutability.
- Added **Goals V2.0 Phase 2A — Goal Lifecycle Foundation** through additive Migration 047: explicit `expired` state, trustworthy future `completed_at` / `archived_at` / `expired_at` transitions, owner-readable immutable deadline history, owner-scoped expiration reconciliation, same-ID deadline extension, and an atomic predecessor-archive/successor-create New Phase RPC in `supabase/migrations/047_goal_lifecycle_foundation.sql` and `types/supabase.ts`.
- Added Active, Completed, Expired, Archive, and Paused Goals views plus an explicit same-identity **Extend Goal** interaction in `features/goals/`; historical Project Goal rows now remain discoverable and route into the corresponding lifecycle view.
- Added focused lifecycle, historical Echo-link, navigation, active-selector, closed-week Momentum-boundary, migration security, and disposable PostgreSQL 16 RPC/RLS validation coverage in `features/goals/*.test.ts` and `features/momentum/service.test.ts`.
- Added **Tracker Metrics — Task 2: period-query index migration.** New `supabase/migrations/046_tracker_logs_period_index.sql` creates a compound covering index `tracker_logs_tracker_id_logged_at_idx` on `public.tracker_logs (tracker_id, logged_at desc) include (id, value)` to serve the tracker-metrics period read path (tracker_id equality + logged_at range/order, satisfiable from the index), and drops the now-redundant single-column `idx_tracker_logs_tracker_id` (a strict leading-column prefix) in the same migration. No column/RLS/data changes; transactional build (no `CONCURRENTLY`, justified by the 37-row table). Applied live via the management API query endpoint (curl UA — Cloudflare 1010 gotcha) and recorded the `supabase_migrations.schema_migrations` row for `046` manually (no Supabase CLI). Verified live: `pg_indexes` shows the new index with the exact expected definition and the old index gone; `schema_migrations` includes `046`; `EXPLAIN` seq-scans at current 37-row volume (expected — the index is future-proofing, not used until the table grows). `npx tsc --noEmit` clean before and after (SQL-only). See `tracker-metrics/audits/002-migration-046-live-verify.md`.
- Added **Tracker Metrics — Task 1: shared timezone-aware cadence utilities.** Extracted the DST-correct zoned-calendar primitives (`normalizeTimezone`, `zonedDateParts`, `toYmd`, `addLocalDays`, `startOfIsoWeekYmd`, `localDateToUtcStart`, `localDateForInstant`) out of `features/momentum/time.ts` into a neutral `lib/time/zoned-calendar.ts` with cached `Intl.DateTimeFormat` instances; `features/momentum/time.ts` now imports and re-exports them so all existing Momentum consumers are unchanged. Added `lib/goals/tracker-cadence.ts` with pure `getPeriodBounds`/`getRecentPeriodBounds`/`isWithinPeriod` (half-open, timezone-aware, daily/weekly-Monday/monthly, invalid-tz → UTC, rejects non-positive/non-integer history counts) plus `lib/goals/tracker-cadence.test.ts` (13 tests: DST spring-forward/fall-back, cross-timezone same-instant, Monday-week boundary, exact-endExclusive exclusion, Dec/Jan + leap-Feb + varying month lengths, 7-bucket contiguity, determinism, count validation). `npx tsc --noEmit` clean; `npm run test:momentum` 64/64 unchanged; cadence tests 13/13. See `tracker-metrics/`. (Uses relative imports intentionally — the `node --experimental-strip-types --test` runner has no `@/` import map.)
- Added **Echo Version 1.0 — UI & Interaction Polish** across `features/entries/components/`, including a Most Recent + Projects library, a persisted collapsible desktop panel, a dominant active workspace, one New chooser, freestyle Quick Reflections, and an intentional OHARA Intelligence Guided Reflection coming-soon state.
- Added additive Migration 044 and `save_entry_v3` so canonical Notes and Reflections can optionally reference one owner-validated Project without duplication, backfill, or destructive legacy-Project changes; expanded `lib/db/entries.ts`, Entries types/validation, and disposable database coverage accordingly.
- Added focused Echo V1 coverage for Project recency/deduplication, Quick-versus-legacy Reflection routing, creation boundaries, inaccessible Guided Reflection behavior, collapse state, caret handling, and the owner-scoped Project trust boundary.
- Added one shared responsive `AppNavigation` toolbar for the authenticated application, preserving the canonical Home, Goals, Echo, Momentum, and Constellation routes plus global creation and account controls.
- Added **OHARA Notes Version 1.1** interaction and export polish, including schema-aware selectable-text PDF generation, private-image rendering, stable reference navigation, guarded contextual selection, and focused release documentation across `features/entries/`, `global.css`, `config/internal-release.ts`, and `docs/NOTES_V1_1_POLISH.md`.
- Added additive Migration 045 for canonical `entries.brt_category` storage and an
  owner-scoped `client_request_id` create key, including legacy same-ID Echo BRT
  backfill and the Project-aware `save_entry_v4` wrapper used by native Reflection
  retries (`supabase/migrations/045_entries_brt_idempotent_create.sql`).
- Added Entries validation/security coverage for the shared `bud | rose | thorn`
  contract, reflection-only classification, UUID request keys, and V4 RPC wiring
  in `features/entries/security.test.ts`.
- Added **Momentum Version 1.1** with deterministic live current-week Goal and OHARA provisional scores, explicit provisional/closed period metadata, stable closed baselines, preserved paused scores, current-week due-boundary handling, cross-version tests, and `docs/MOMENTUM_V1_1_IMPLEMENTATION.md` in `features/momentum/`, the Momentum APIs, and Migration 043.
- Updated the guarded loopback integration and authenticated API smoke assertions for V1.1 provisional responses and immutable single-close behavior in `scripts/momentum-local.integration.mjs` and `scripts/momentum-api.smoke.mjs`.
- Clarified `features/momentum/legacy-phase1.ts` so the historical `momentum-v1.0` replay implementation cannot be mistaken for the current V1.1 production path.
- Completed **OHARA Notes Version 1.0** with contextual inline Goal/Intelligence reference menus, panel-level jump/open/remove actions, keyboard-focusable anchors, an explicit document-sheet layout, and `docs/NOTES_V1_IMPLEMENTATION.md`.
- Added the temporary config-driven internal “What’s New” login modal at the root auth lifecycle, with a single `SHOW_INTERNAL_RELEASE_NOTES` flag, session-scoped SIGNED_IN/SIGNED_OUT behavior, focus containment/restoration, Escape/backdrop/X dismissal, and focused auth lifecycle tests.
- Rebuilt Notes V1 on a ProseMirror/Tiptap 2 document model with conventional H1/H2/H3, bold, italic, underline, strikethrough, bullet/numbered/task lists, block alignment, links, undo/redo, private image nodes, stable node IDs, lightweight Goal references, live embedded Goal cards, and selection-based OHARA Intelligence anchors in `features/entries/components/`, `features/entries/editor-document.ts`, `features/entries/types.ts`, and `global.css`.
- Added additive Migration 042 with schema-V2 tracking, a private owner-scoped `note-images` bucket, document-derived/server-validated current Goal progress evidence, canonical completion-transition events, and an atomic revision-checked `save_entry_v2` wrapper; clients cannot invoke the evidence synchronizer directly, and Notes never writes Momentum scores or snapshots.
- Added editor document compatibility, reference extraction, persistence, validation, source-level security coverage, and a disposable PostgreSQL Migration 042 trust-boundary harness in `features/entries/*.test.ts` and `scripts/notes-editor-security*`.
- Added OHARA Momentum V1 as two deterministic, server-authoritative 0–100 metrics: `goal-momentum-v1.0` for each active Goal and `ohara-momentum-v1.0` for portfolio movement, with centralized category/mode configuration and deterministic `difficulty-v1.0` Goal Difficulty profiles in `features/momentum/`.
- Added additive migrations `040_momentum_v1.sql` and `041_momentum_v1_recalculation_baseline.sql` for Goal Difficulty, immutable/versioned Goal Momentum snapshots, bounded OHARA V1 snapshot metadata, service-role-only publishers, idempotent hashes, superseding revisions, RLS, and protected recalculation baselines while preserving Migration 038 and historical Phase 1 records.
- Added the authenticated owner-scoped `/api/momentum/goals/:goalId` contract, shared user-scoped Momentum client cache/selectors, the canonical specification copy, and `docs/MOMENTUM_V1_IMPLEMENTATION.md`.
- Expanded unit, trust-boundary, disposable database, and loopback integration coverage for the V1 formulas, all categories/modes, inactivity, missing-data reweighting, reflection/recovery caps, portfolio weighting, revisions, timezone boundaries, idempotency, forgery rejection, immutable records, and cross-user isolation.
- Expanded the authenticated loopback API smoke to cover both overall and owner-scoped Goal Momentum endpoints, missing-Goal non-disclosure, and unauthorized access rejection.

### Changed
- Shipped **Home load performance — de-waterfall + SWR (Stages 1–3).** Reworked how Home (`/dashboard` — Today's Focus + Next Step) loads its goal-derived data. **Stage 1:** `fetchGoals` now runs the `reconcile_goal_expiration_v1` maintenance write fire-and-forget instead of awaiting it before the goals `SELECT` (self-healing, off the read path), and `useGoals` became stale-while-revalidate over the goal store — instant cached paint on return-nav, background refresh, in-flight dedup, `userId`-keyed so a user switch never serves the prior user's goals (`features/goals/services/goal-service.ts`, `features/goals/hooks/useGoals.ts`). **Stage 2:** the two goal-signal reads that previously waterfalled *after* the client goal load were folded into the new `/api/home/summary` aggregator (see Added), retiring `features/goals/services/active-goal-reflection-service.ts` and the `app/api/goals/weekly-task-counts+api.ts` route (`weekly-task-count-service.ts` reduced to its shared types). **Stage 3:** the presentation moved into a self-contained `features/goals/components/home/` module (`TodayFocusSummary`, `NextStepPanel`, `TodayFocus` composer), shrinking `app/(app)/dashboard.tsx` 644 → 266 lines to thin composition; kept in `features/goals` (not a `features/home` slice) to respect the no-cross-feature-import rule. Updated `features/tasks/architecture.test.ts` to assert the new aggregator seam while keeping the never-read-legacy-trackers guardrail. **Benchmark** (live Supabase medians, server round-trip critical path): cold signals-ready 717 → 263 ms (−63%), first paint 472 → 263 ms (−44%), return-nav 717 → ~0 ms cached (−100%) — recorded in `docs/PERFORMANCE_BASELINE.md` (2026-09-18). New "Home Data Flow" rule added to `docs/CLAUDE.md`. `npx tsc --noEmit` clean; goals + tasks suites pass. Pushed to `main` (commits b60cff1, 87f218d, 80cd85a). Follow-up: `docs/API_CONTRACT.md` still documents the removed `GET /api/goals/weekly-task-counts` route — pending a doc fix.
- Simplified the desktop account-menu information architecture by removing the duplicated Friends section heading and Friends rail item, making the Friends count the direct entry point to the existing Friends pane, and retaining Requests and Add People as the people-management actions. Removed the duplicate Dark Mode control from the Settings pane because appearance remains available in the account rail (`features/friends/components/FriendsPopover.tsx`, `features/friends/components/StatCell.tsx`, and `components/layout/SettingsModal.tsx`).
- Integrated personal profile editing into the desktop account menu: selecting the avatar/name now opens username, bio, timezone, interests, and profile-picture controls in the existing menu-builder panel instead of launching the legacy nested modal. Extracted a shared `AccountPane` so the compact/mobile account modal keeps the same persistence and validation behavior (`components/layout/AccountModal.tsx`, `components/layout/AvatarMenu.tsx`, and `features/friends/components/FriendsPopover.tsx`).
- Matched the Home / Goals / Echo / Momentum / Constellation navigation labels to the OHARA wordmark’s Lora editorial font, and compacted the scrollable Goals selection rail by hiding Goal descriptions there while preserving descriptions in the selected Goal detail (`components/layout/AppNavigation.tsx` and `features/goals/components/GoalsWorkspace.tsx`).
- Changed the Goal countdown’s end-date workflow to match the Goals “More” interaction: the ellipsis now opens a one-action “Edit date” popover, which launches the shared calendar immediately and saves its applied date without the former intermediate END DATE field and second save step. Added optional externally controlled/open-without-trigger support to the shared date picker for this transition (`features/goals/components/CountdownTimer.tsx` and `components/ui/DatePicker.tsx`).
- Relocated the live “Goal ends in” countdown from the Overview tab to its own panel directly beneath the Goal header’s Category / Started / End date metadata, keeping the existing end-date editing and read-only rules while making the countdown visible independently of the selected Goal tab (`features/goals/components/GoalsWorkspace.tsx`).
- Changed Echo's header `+ New` action into an anchored, dismissible, accessible Note/Reflection menu that hands the selected type to the existing creation builder (`features/entries/components/EchoNewMenu.tsx` and `EntriesScreen.tsx`).
- Tightened the Reflection-to-Circles seam in `lib/db/circles.ts`: the owner-only link picker now receives only each Reflection ID/title and an empty description, never private `plain_text` or `takeaway`; sharing a description requires deliberate author input in the post composer.
- Updated the Echo creation and Quick Reflection experience in `features/entries/components/EchoCreationModal.tsx` and `QuickReflectionEditor.tsx` with explicit private-by-default language, truthful live save status, 44px icon targets, clearer accessibility labels/hints, input limits matching the API, and a navigation guard that keeps an unsaved native Reflection open when persistence fails. New Entry creates now reuse a valid owner-scoped UUID request key across retries.
- Made the Echo All/Notes/Reflections selection route-backed across `EntriesScreen.tsx`, `EntriesLibrary.tsx`, and compatibility routes so the active view survives responsive remounts, Entry navigation, creation, and deep links; added filter-specific empty states and accessible result/tab announcements.
- Reconciled the Goals V2.0 release with upstream Entries and Tracker Metrics work: retained canonical Entries BRT/idempotent creation as Migration 045 and the production-applied Tracker-log index as Migration 046, renumbered the pending lifecycle/Task chain to Migrations 047–051, updated all release tests/docs/harnesses, and expanded the disposable Entries database harness to execute and verify `save_entry_v4` BRT persistence plus create-retry idempotency (`supabase/migrations/045_entries_brt_idempotent_create.sql`, `supabase/migrations/047_goal_lifecycle_foundation.sql` through `051_tasks_release_cutover_controls.sql`, `scripts/notes-editor-security-*`, `scripts/entries-brt-idempotency.test.sql`, and `scripts/tasks-*`).
- Refactored Migration 049's already validated legacy mapping statements into the reusable `run_tasks_legacy_catchup_v1` release function so the initial backfill and final post-deployment catch-up share one implementation and retain the same baseline, recurrence, provenance, and no-inference rules.
- Changed Goal creation, Goal detail, Home Today’s Focus, and Home next-action flows to use canonical Tasks and Task Occurrences. The OHARA Intelligence preview now sits directly above the standalone Tasks card, and the former Tracker-backed Tasks tab expands the same canonical surface rather than exposing a competing implementation.
- Removed the final legacy Tracker-derived “Next step” fallback from Home Goal previews; Milestones remain eligible there while actionable Task work is sourced only through canonical Task surfaces.
- Changed New Phase continuation to copy only active recurring Task definitions, pending one-time Tasks, compatible pending Milestone links, and current schedule configuration into fresh successor IDs. Historical/completed/missed occurrences, quantities, provenance, logs, and legacy Tracker rows remain exclusively on the predecessor.
- Preserved legacy `current_value` as baseline metadata only; preserved weekly/monthly/null cadence without guessing dates; generated forward schedules only for trustworthy daily imports; and kept every source Tracker, tracker log, and action log intact and traceable.
- Documented the safe future production sequence for the observed `044, 046` migration ledger: add the exact production 046 locally, verify `supabase db push --include-all --dry-run` presents 045 and later migrations while skipping 046, then use the approved `--include-all` push only during a separately authorized production deployment. No production migration was run in Phase 2B implementation.
- Changed primary Goal, Home, Echo picker, Project, and provisional Momentum consumers to treat `status == active` as the explicit current-pursuit boundary. Project membership and historical Goal records remain intact, while inactive Project Goals are separated into Goal History and Constellation counts/DTOs now recognize `expired` without altering legacy states.
- Changed New Phase persistence in `lib/db/goals.ts` from separate client-side inserts to one owner-checked transactional database operation that preserves the predecessor/successor relationship, carries pending milestones and reset trackers, and archives the predecessor only if the successor operation succeeds. Normal deadline edits continue on the original Goal ID and now retain prior deadline values.
- Changed closed-week Momentum source selection to reconstruct the deadline effective at week close before evaluating historical eligibility, alongside lifecycle timestamps and repaired-phase successor creation times; provisional scoring remains exact-active and no scoring weights or formulas changed.
- Polished the canonical Goals workspace across `GoalsWorkspace.tsx`, `GoalDetailHeader.tsx`, `CountdownTimer.tsx`, `MilestonesPanel.tsx`, `TrackersPanel.tsx`, and `ExtendGoalModal.tsx`: normalized the feature-title hierarchy, preserved the three-column behavior, composed the center and side columns into sibling premium surfaces, and replaced nested elevation with shared spacing and neutral section dividers.
- Corrected the enabled Goal phase-extension action and all three New Phase modal steps to use the current semantic `#63C174` action family, shared themed surfaces, normalized typography, inputs, borders, radii, hover/pressed/disabled states, and readable light/dark contrast without changing lifecycle or persistence logic.
- Validated the Goals polish with authenticated light/dark and compact-width browser review, non-mutating Goal selection/search/filter/tab/menu/New Goal checks, all three phase-flow steps without submission, 205 focused Goals/Momentum/Entries/Constellation/Auth tests, a clean-source TypeScript pass, the 51-route Expo web export, and `git diff --check`; no lint script is configured and the normal typecheck continues to report only the protected duplicate editor's three pre-existing diagnostics.
- Normalized the application-wide typography foundation in `constants/design.ts`, `components/ui/Typography.tsx`, and `tailwind.config.js` around the bundled Inter family and semantic meta/caption/body/control/card/section/page/editor/chart roles; Lora remains limited to intentional brand and reflective/editorial moments.
- Increased shared navigation, button, input, Echo library/editor, Home card, Goals milestone/tracker, Momentum, modal, and Constellation inspector typography with proportional control/row sizing and normalized line heights, while retaining compact chart and graph-label exceptions where geometry requires them.
- Updated `global.css` so Notes editor chrome, menus, references, embedded Goal metadata, and long-form content resolve through the same canonical Inter system with readable control and supporting-copy sizes.
- Documented the new semantic scale and font ownership in `design/OHARA_Design_System_v1.md` and `components/CLAUDE.md`.
- Completed the narrowly scoped **Echo Version 1.0 — Final Cleanup** across `NoteEditor.tsx`, both `RichTextEditor` implementations, and `global.css`: the formatting toolbar now owns the full-width top band, while the writing canvas and non-shrinking 328px Ohara Intelligence card become siblings below it. The inset panel starts lower, ends shorter, falls back to the existing sheet below the safe 1020px workspace threshold, and returns all body width when closed.
- Kept the Note formatting toolbar on a 960px editor-container query with bounded width, compressed controls, and contained horizontal overflow, preserving every command and the existing More menu without allowing Intelligence to enter or cut off the toolbar band.
- Replaced the Echo library's raw transport error rendering with a semantic, non-technical error card and Retry action, preserving genuine failure visibility without exposing `Load failed` as product copy.
- Standardized the canonical application accent on vibrant OHARA green `#63C174` for light and dark themes in `constants/colors.ts`, added shared `#4EAA60` hover/pressed behavior in `Button.tsx`, and derived selected/hover/focus surfaces from the same RGB family in `global.css`. The application mark, navigation, Echo controls, editor states, links, Projects, and Intelligence accents now inherit the semantic family; intentional BRT/category, feedback, status, illustration, and neutral structural colors remain distinct.
- Validated the Echo final cleanup with 54 Entries/Echo source tests, a clean-source TypeScript pass, the 51-route production Expo web export, and `git diff --check`; no lint script is configured, and standard typecheck output remains limited to the protected pre-existing duplicate editor diagnostics. Authenticated browser state checks remain a manual follow-up because the isolated browser reached the local login boundary and Chrome was not running/connected.
- Tightened Echo V1's page hierarchy in `EntriesScreen.tsx`: restored the canonical Echo mark, removed the persistent subtitle and generic oversized feature header, aligned one proportionate `New` control with the Echo title/workspace, and reduced shared-token vertical gaps below application navigation.
- Replaced Echo's single outer slab with separate bordered, rounded, subtly elevated library and active-workspace cards; refined `EntriesLibrary.tsx` spacing and placed Projects in a deliberate inset group while preserving search, filters, selection, Project navigation, and all existing data behavior.
- Reframed the empty workspace as a compact inset state without a duplicate creation button, and changed `NoteEditor.tsx` from a blurred utility rail to a softly tinted, rounded Ohara Intelligence companion card using the canonical mark while preserving reference, link, close/reopen, and non-AI behavior.
- Moved the Echo collapse control into `EntriesLibrary.tsx`, replaced the dedicated 42px divider rail with a temporary left-edge restore overlay, and added an explicit Most Recent Project-exit action that preserves an open Entry while clearing only Project context.
- Widened the Notes sheet from 960px to 1280px with a controlled 900px reading measure, reduced outer workspace gutters to 10–18px, increased desktop body typography from 17px/1.65 to 18px/1.68, and added 820px/600px editor-container queries so library and Intelligence panel changes immediately rebalance the canvas.
- Validated the focused Echo layout polish with 50 Entries/Echo source tests, the clean-source TypeScript configuration, the 51-route production Expo web export, and `git diff --check`; the standard typecheck continues to report only the protected untracked duplicate editor's three pre-existing diagnostics, and no lint script is configured.
- Replaced the live Entries category/date-shelf experience with Echo's editorial reverse-chronological library and optional Project folders while retaining a quiet All/Notes/Reflections filter with Notes as the default; search, canonical IDs, existing content, and legacy Reflection readability remain intact.
- Routed global and Echo creation through the unified New flow, preserved the completed Notes editor inside the expanded workspace, and added optional Goal/Project organization to Notes and Quick Reflections. The old guided/chat implementation is retained for future assessment but is no longer exposed as the normal Reflection path.
- Updated `store/uiStore.ts`, `store/clearAllStores.ts`, and `global.css` for stable collapse persistence, responsive list/detail behavior, and non-editable workspace caret suppression without disabling text selection in actual editors.
- Extended the unified Entries record/draft contract with canonical BRT and
  optional idempotency fields, and routed create/update persistence through
  `save_entry_v4` while preserving Project support and BRT when older desktop
  update payloads omit it
  (`features/entries/types.ts`, `features/entries/validation.ts`,
  `lib/db/entries.ts`, and Reflection completion state).
- Updated Home, full Momentum, Goal Momentum, and Goal Analytics surfaces to label the current local week as provisional; successful tracker, action, milestone, Notes progress-anchor, Goal-progress, and qualified goal-linked Reflection writes now request one best-effort shared Momentum refresh without coupling domain-write success to recalculation success.
- Updated the temporary internal What's New content for Momentum Version 1.1 and retained the existing Notes Version 1.0 and Momentum beta release history.
- Refined the Notes writing workspace to a 960px document sheet with approximately 740px maximum reading width, responsive 80–110px desktop/40–64px tablet/18–28px mobile page padding, a restrained page border/shadow, sticky separated toolbar, 17px long-form body typography, and explicit light/dark semantic tokens.
- Expanded Goal Reference management with Open Goal, Change Goal, progress-evidence settings, and source-preserving removal; expanded OHARA Intelligence Reference management with open, question/context edit, and source-preserving removal from both inline anchors and the panel.
- Replaced the deprecated `document.execCommand`/single-HTML-blob web implementation with centralized Tiptap commands and active selection state; legacy V1 block/HTML notes remain readable and upgrade in place only after an editor change, while note-level Goal links remain separate from inline references.
- Expanded the existing OHARA Intelligence rail with bidirectional inline-reference navigation, excerpts, action/question metadata, linked Goal references, progress state, removal controls, and an honest premium-locked response state with no fabricated AI output.
- Made Notes saves schema-aware and revision-conditional, while preserving the existing 900 ms debounce, Saving/Saved/Error states, local unsaved-draft recovery, and relationship behavior in `features/entries/components/NoteEditor.tsx`, `features/entries/validation.ts`, and `lib/db/entries.ts`.
- Kept the V2 document contract platform-neutral and made the native fallback data-safe: plain V2 notes remain editable as structured paragraphs, while rich V2 notes render read-only instead of being silently flattened into legacy text until native rich-editor parity is implemented.
- Added a quiet `(Version 1.0)` label beside the authenticated Momentum workspace title in `app/(app)/momentum.tsx` so the implemented model version is visible without changing calculations or API behavior.
- Replaced active production uses of the experimental Phase 1 equation with the bounded Goal/OHARA V1 engines while isolating the historical five-pillar/unbounded formula in `features/momentum/legacy-phase1.ts` solely for reproducibility.
- Updated Home, the expanded Home Momentum view, the full Momentum workspace, shared Goal Momentum cards, and canonical Goals workspace analytics to use authoritative V1 values and V1-only histories; removed authenticated Momentum preview/sample values and kept Goal progress visually and semantically distinct from Goal Momentum.
- Updated the trusted Momentum service to normalize owner-scoped Goals, actions, milestones, trackers, tracker logs, and goal-linked reflections into stable local-week evidence; raw reflection text stays inside the qualification boundary and is not returned or persisted in V1 diagnostic payloads.

### Fixed
- Restored legacy manual Reflections to the freeform editor instead of the chatbot-style completed view. Migration 036 created one synthetic user-only turn for imported Echo entries; `isQuickReflection` now requires an actual OHARA turn before classifying a Reflection as guided, preserving genuine guided conversations without rewriting private content (`features/entries/utils.ts`, focused tests, and `docs/ECHO_PIPELINE.md`).
- Fixed a remaining Echo detail-navigation case where opening a Reflection could visually reactivate Notes: row navigation now carries the record's explicit type, and detail screens recover a missing `view` parameter from the already-loaded Entry before synchronizing the route (`EntriesScreen.tsx`, `EntriesLibrary.tsx`, and `features/entries/utils.ts`).
- Fixed the Echo Reflections control reverting to Notes by removing the library-local reset effect and making `view=reflection` the stable source of truth. Reflection compatibility/deep-link routes now carry that view through the unified Entries workspace.
- Fixed Task write-path races and ambiguity with database-enforced stable occurrence identity, insert-first idempotency serialization, atomic quantity deltas, controlled binary undo, non-negative quantity constraints, one-time schedule replacement cancellation, and direct-write-denying RLS.
- Protected Task history from reinterpretation by rejecting completion-mode changes after completed/skipped/missed occurrences exist and by making superseded schedule occurrences non-mutable through trusted occurrence RPCs.
- Fixed inactive Goal Task reads so completed/archived/expired Goal history remains visible without attempting reconciliation, while every Task mutation still rejects unless the parent Goal is explicitly Active.
- Fixed Task and Today’s Focus reads to surface schedule-reconciliation failures instead of silently returning an incomplete occurrence projection.
- Applied the narrow Tasks V2 migration switch consistently across Task reads and trusted mutation endpoints so the cutover can be disabled without leaving partial write paths active.
- Reset Task creation/edit drafts when their modal closes so reopening the same Task or adding another Task cannot reuse stale form state.
- Matched archived Task row controls to the server lifecycle boundary so historical Task completions and quantities no longer appear mutable on an otherwise active Goal.
- Kept imported Tracker-log occurrences immutable and surfaced unscheduled weekly/monthly/null legacy definitions as reviewable Anytime rows with preserved baseline context, without fabricating occurrences or recurrence dates.
- Froze normal application writes to `trackers`, `tracker_logs`, and `action_logs`: legacy mutation endpoints now return `410`, while the tables and read-only compatibility paths remain available for rollback, audit, historical views, and parity comparison.
- Fixed expired, completed, and archived Goals contaminating current-work surfaces; fixed the duplicate-active-phase path by repairing existing active predecessor/successor pairs and preventing future non-atomic successors; fixed Entry autosaves so a requested historical Goal link and its existing Note progress anchor survive archival without permitting a new link to an unrelated inactive Goal.
- Fixed the Constellation Goal-link editor race in `ConstellationGoalLinkPanel.tsx` by initializing the controlled edit draft before rendering edit mode, ensuring note updates replace existing text while preserving the same link record, mutation path, and persistence semantics; expanded `goal-link-state.test.ts` replacement assertions.
- Raised the light-theme muted text token to an AA-readable 4.65:1 on white and the special dark-surface muted token to 6.58:1, preventing small timestamps/helper copy from becoming nearly invisible while preserving secondary hierarchy.
- Fixed Project browsing feeling like a trapped state by exposing a contextual back action inside the Echo library; Project folders open as combined Note/Reflection views and normal Most Recent browsing restores the Notes default.
- Fixed Quick Reflection autosave follow-up scheduling so edits made while an earlier save is in flight are queued for the next revision instead of being left only in local recovery storage.
- Made ambiguous canonical Entry creates safe to retry without duplicate owner
  rows, retained BRT across Reflection completion updates, and classified a BRT
  supplied for a non-Reflection Entry as a client validation error rather than a
  server failure.
- Fixed inactivity being presented as unavailable by retaining established Goal/OHARA scores with `paused`, reserving `building` for users without a baseline and `unavailable` for technical failures.
- Fixed qualified Reflection occurrence normalization so a completion uses `completed_at` and ordinary later editor autosaves do not move old evidence into the current week.
- Fixed the V1.0-to-V1.1 closed-week transition in `supabase/migrations/043_momentum_v1_1_cross_version_baseline.sql` so trusted publishers validate against the latest earlier immutable snapshot across algorithm versions without rewriting V1.0 history.
- Restored visible bullet and numbered markers after Tailwind Preflight’s global `list-style: none` reset, including explicit theme-aware marker colors, indentation, and nested marker styles.
- Replaced browser-dependent task checkbox appearance with an explicit accessible unchecked outline, restrained checked state/checkmark, focus state, and checked-text treatment in both themes.
- Preserved checked document items, Goals, immutable progress events, and historical Momentum state when a Goal Reference is removed; the disposable Migration 042 harness now proves current evidence is removed without rewriting history.
- Replaced ambiguous editor symbols and nonfunctional pseudo-checkbox insertion with conventional accessible controls, structural task items, preserved toolbar selections, standard keyboard commands, safe schema-based paste handling, and responsive overflow behavior.
- Scoped the shared Momentum response cache by authenticated user ID to prevent stale cross-account reuse.
- Corrected first-week OHARA V1 recalculation baseline handling through additive Migration 041 and made local integration fixtures repeatable and non-destructive by using unique users/source IDs plus dates relative to the current local week.
- Kept the authoritative 0–100 Home trend visible when only the first V1 weekly snapshot exists, instead of replacing a valid one-point history with an unavailable message.
- Prevented the closed Goal-extension modal from repeatedly resetting state in `features/goals/components/ExtendGoalModal.tsx`, eliminating the maximum-update-depth crash when a Home Goal opens the canonical Goals workspace.

### Added
- **Canonical Goal navigation and compatibility coverage:** added one encoded `/goals?goal=<id>` destination, legacy `selected` query compatibility, and focused tests for deep-link selection and URL encoding.
- **`features/goals/components/GoalsWorkspace.tsx`, `features/goals/goals-workspace.ts`, and focused tests:** added the full authenticated Goals workspace with real search/status/category filtering, persistent Goal selection, responsive list/detail/context regions, Goal overview tabs, canonical category identities, and deterministic selector coverage.
- **Home Echo previews:** added focused Note and Reflection selectors/hooks so the Dashboard renders the most recently updated real Note and Reflection independently, including explicit linked-Goal context, honest empty states, existing create/open routes, and regression tests preventing content-type or inferred-link bleed.
- **Home Goal activity:** added an owner-scoped dashboard activity read for completed actions and canonical Entries joined through `goal_id`, with focused tests proving unrelated Goal activity cannot enter another Goal preview.
- **`components/ui/brand-icons/` and `previews/brand/`:** added the three supplied `currentColor`-compatible Home, Momentum, and Constellation marks plus a development-only review surface showing those additions alongside the retained logo family at 16/20/24/32/48px in representative light/dark product contexts.
- **`features/echo/dashboard-latest-entry.ts`, `features/echo/services/echo-service.ts`, `features/echo/hooks/useDashboardLatestEntry.ts`, `lib/events/entries.ts`, and `lib/diagnostics/performance.ts`:** added a dashboard-only latest-entry summary read that uses the restored session user ID, explicitly selects only canonical Entries `id`, `plain_text`, and `updated_at`, orders newest-first, limits the result to one row, maps no rows to the existing empty state, bounds the client DTO preview, refreshes after confirmed Notes/Reflections mutations without loading the full Entries store, and reports only aggregate one-request timing metadata.
- **`features/echo/dashboard-latest-entry.test.ts`:** added focused coverage for dashboard DTO mapping, bounded previews, no-row handling, and query-error handling.
- **`features/goals/active-goal-selectors.test.ts`:** added focused coverage for active-only filtering, latest-reflection ordering, deterministic null-timestamp fallback, input immutability, and project-title resolution.
- **`lib/diagnostics/performance.ts`, `app/_layout.tsx`, `app/(app)/dashboard.tsx`, `features/goals/hooks/useGoals.ts`, `features/goals/services/goal-service.ts`, `features/projects/store.ts`, `features/echo/hooks/useEntries.ts`, and `features/echo/components/EchoScreen.tsx`:** added development-only or explicitly enabled aggregate performance timing for the Phase 1 dashboard and Entries bootstrap paths, including safe result/request counts and initial-load versus refresh metadata. The diagnostics deliberately exclude record content, identifiers, credentials, and error payloads.
- **`docs/PERFORMANCE_BASELINE.md`:** documented enabling and reading the scoped diagnostics and recorded the production web-export size baseline for later optimization comparisons.
- Added a canonical `FeaturePageHeader` for Goals, Entries/Echo, Momentum, and Constellation, including the existing Echo mark and the Sidebar-established Constellation mark in semantic OHARA green.
- Extended the authenticated Momentum response with owner-scoped weekly snapshot history, exposing only the latest immutable revision per algorithm-version/week in chronological order.
- Added a real authenticated `/goals` index route with an intentionally restrained Coming Soon surface and the existing Create a Goal flow; goal-detail and creation routes remain unchanged.
- Added `docs/LOCAL_MANUAL_REVIEW_001_039.md` and local browser screenshots documenting the authenticated post-migration manual review, runtime/API evidence, database/security checks, classified findings, and readiness recommendation.
- Added `supabase/migrations/039_restore_explicit_table_privileges.sql` with an explicit final-state PostgREST privilege matrix, no anon table CRUD, preserved friendship/Momentum mutation revokes, and enumerated service-role access for clean local installs.
- Added `docs/SUPABASE_MIGRATION_CHAIN_REPAIR.md` with root cause, deployment evidence, baseline-versus-forward repair rationale, catalog checks, local validation, risks, and a staged rollout procedure.
- Added the versioned `momentum-v1.0` calculation foundation, local-timezone weekly boundaries, normalized/deduplicated task events, explainable diagnostics, focused tests, open-decisions document, and private snapshot/event schema in migration 038.
- Added the authenticated `/api/momentum` service boundary and Home summary hook for real Momentum value/change, Weekly Streak, and Tasks Completed This Week metrics.
- Added a loopback-only Supabase configuration/target guard, ignored local CLI state, disposable PostgreSQL migration-security harness, local Auth/PostgREST integration fixtures, actual Momentum API smoke test, and local-development guide.
- Integrated the upstream unified Entries workspace, latest-entry invalidation pipeline, active-goal reflection selectors, supporting tests, and migration 037 from `upstream/main` without merging unrelated branch changes.
- Added shared spacing, radius, control, layout, and cross-platform elevation tokens in `constants/design.ts` to establish a consistent UI foundation from the OHARA design system and reference mockups.

### Changed
- Validated the application-navigation and Home-layout pass with 213 focused source tests, the 51-route production Expo web export, and `git diff --check`; the only full-repository type diagnostics remain in the protected pre-existing untracked `RichTextEditor.web 2.tsx` duplicate.
- Replaced the desktop left sidebar with a restrained full-width top application toolbar and a compact two-row small-screen treatment. Removed persisted collapse state, sidebar sizing tokens, shell row geometry, legacy Echo width subtraction, and Constellation rail-dependent breakpoints; the standalone Goal creation flow now shares the same navigation.
- Removed Home's outer dashboard surface so its real greeting and existing data-backed cards render directly on the normal page background with wider, normalized responsive gutters.
- Updated **OHARA Notes Version 1.1** to keep its 960px decorative document sheet outside the editable writing surface, expose genuine-selection Goal/Intelligence actions while the Intelligence panel remains open, reuse the canonical shared Goals mark, and replace the ambiguous numbered-list icon with a conventional numbered glyph.
- Replaced Notes/Reflection plain-text PDF export with a schema-aware, selectable-text, multipage PDF renderer that preserves heading hierarchy, inline formatting, alignment, links, list/checklist semantics, private signed images, printable Goal references, and immutable authored Note content.
- Updated the temporary internal What's New configuration and focused Auth/Entries tests for the concise **OHARA Notes Version 1.1** release.
- **`features/goals/components/GoalsWorkspace.tsx` and existing Goal-detail components:** consolidated the approved three-column Goals shell with the mature Goal hero, deadline timeline, editable milestones, interactive trackers, activity, project move, description, completion, and archive behaviors. The contextual rail now shows confirmed-or-honest Intelligence, real count-based analytics, explicitly unavailable recommendations, and only explicitly linked recent Notes/Reflections.
- **Goal entry points across Home, Today, project rows, Settings, Momentum analysis, continuation flows, and Goal creation:** route into the canonical `/goals?goal=<id>` workspace. The legacy `/goals/[id]` reader now forwards bookmarks to that workspace while distinct creation and Vault routes remain unchanged.
- **Verification:** rendered the authenticated canonical workspace in light and dark with real Goal data, confirmed selection persistence after refresh, legacy deep-link forwarding, interactive milestone/tracker controls, and zero error-level browser logs. Seven focused Goal/routing tests, 13 Entries tests, the known-valid source type-check, web export, and `git diff --check` pass.
- **`app/(app)/goals/index.tsx` and the new Goals workspace:** replaced the restrained Coming Soon page with the approved journey-oriented composition backed only by existing Goals, milestones, trackers, activity, Notes, and Reflections. Existing Goal creation/detail routes and workflows remain unchanged; unavailable progress history, tasks, insights, or supporting data render honest states instead of mock content.
- **`features/entries/components/EntriesLibrary.tsx` and `features/entries/utils.ts`:** made every populated Echo/Entries shelf initially expanded while preserving per-shelf manual collapse choices across filter and sort changes; each shelf now promotes its own newest Note and newest Reflection by canonical `updatedAt`, deduplicates them by stable ID, and leaves the remaining entries in the user-selected Recently edited or Title A–Z order.
- **`app/(app)/dashboard.tsx`:** reorganized the approved Home composition without changing its visual system: the most recently updated Project now occupies the former lower-left Echo slot, while the former full-width Projects region is now two equal Echo Notes and Echo Reflections cards. Project, Note, Reflection, Goal, and creation navigation remain on their existing routes.
- Project, Note, and Reflection dashboard recency uses the existing `updated_at` field because the current schema has no access/open timestamp; no schema, API contract, Momentum behavior, authentication, or Supabase configuration changed.
- **`components/ui/BrandIcon.tsx`, Sidebar, authenticated feature headers, Home cards, Global Create, Entries, Constellation actions, and landing previews:** applied only the supplied new Home, Momentum, and Constellation vectors while preserving the previous visual assets for Goals, individual Goals, Today’s Focus, Echo, Echo Entry, and Projects.
- Replaced Home’s generic house, Momentum’s feature-level trend arrow, and Constellation’s feature-level network symbol with the supplied marks; retained functional navigation/editing/data-trend icons, the previous Ohara Intelligence sparkle, the OHARA master logo, theme-mode asset, and Goal-category colors.
- **`features/entries/components/EntriesScreen.tsx`, `EntriesLibrary.tsx`, `ReflectionLauncher.tsx`, entry detail/editor routes, `app/(app)/echo.tsx`, `app/(app)/_layout.tsx`, `features/entries/utils.ts`, `store/uiStore.ts`, and `store/clearAllStores.ts`:** replaced the Notes/Reflections tab split with one mixed Entries workspace, distinguished Notes and Reflections by symbols on shared cards, hid empty categories, made every populated category shelf collapsed by default, compacted the four reflection starting points into a hover/focus description launcher, and removed obsolete tab state and tab-specific return URLs so legacy Echo links and global creation converge on the unified pipeline.
- **`app/(app)/dashboard.tsx`, `features/echo/hooks/useQuickEntry.ts`, and `features/entries/store.ts`:** removed the dashboard's legacy `useEntries()` dependency so mounting the dashboard no longer fetches all Echo entries, container links, picker goals, or Echo folders; the Recent Entry card now reads the redesigned Entries workspace independently and refreshes after canonical Note/Reflection create, update, or delete operations.
- **`app/(app)/dashboard.tsx`, `features/goals/active-goal-selectors.ts`, `features/goals/services/active-goal-reflection-service.ts`, `features/goals/store.ts`, and `lib/diagnostics/performance.ts`:** made the existing goal-store result the dashboard's canonical goal dataset, derive and deterministically order active goals without mutating the shared array, and replace the duplicate legacy Echo-enriched goal pipeline with one optional client-safe reflection-timestamp read from canonical `entries`/`entry_goal_links`, scoped to active goal IDs. Reflection ordering can update Today's Focus after primary content renders, while the goal store now begins its first session load in a loading state to prevent an empty-dashboard flash.
- **`app/_layout.tsx`, `app/(app)/dashboard.tsx`, `app/(app)/momentum.tsx`, `components/layout/GlobalCreateControl.tsx`, `components/layout/Sidebar.tsx`, `components/ui/TodayGoalCard.tsx`, `features/constellation/components/ConstellationActionMenu.tsx`, `features/echo/components/EchoContainerTree.tsx`, `features/echo/components/EchoScreen.tsx`, `features/friends/components/AddPeoplePane.tsx`, `features/friends/components/FriendsPopover.tsx`, `features/goals/components/EchoTrail.tsx`, `features/goals/components/GoalCard.tsx`, `features/goals/components/GoalEchoAnalysisCard.tsx`, `features/goals/components/ProjectGoalRow.tsx`, and `features/goals/components/VaultItemCard.tsx`:** replaced Google-font and Ionicons package-barrel imports with direct variant/family modules so production web exports omit unused font weights and icon families without changing the registered font-family mapping or icon behavior.
- Refined the active Echo note-detail workspace in `features/entries/components/NoteEditor.tsx` and both `RichTextEditor` platform implementations: document text now uses an 18px/1.65 long-form rhythm, the web reading measure expands from a padded 760px box to a responsive 900px measure, headings/lists/quotes have clearer hierarchy, linked-goal metadata uses 14px type and 32px chips, and formatting controls use grouped 40px targets with 19px icons.
- Made Ohara Intelligence open by default while preserving its existing close/reopen behavior, 328px desktop width, narrow-screen modal behavior, linked context, related entries, and honest preview copy; the panel now uses the canonical semantic green through translucent, blurred glass surfaces rather than a separate color family.
- Preserved note persistence, autosave, editor commands, routing, linking, export/share, authentication, and backend behavior while improving light/dark contrast, caret/selection styling, responsive document margins, and icon-button accessibility labels/roles.
- Visually verified the authenticated note at the available 1483×960 capture viewport in light/dark mode with Intelligence open and collapsed; confirmed reload-default opening, 900px re-centering when hidden, and no fixture-content mutation. Entries (8) and Echo composer (5) tests, the known-valid source type-check, the 50-route web export, and `git diff --check` pass.
- Replaced the full Momentum page's hardcoded Mon–Sun sample graph, forced SVG stretching, fake `+8%`, and 0–100 axis with a measured responsive chart of real weekly snapshots on the algorithm's open-ended non-negative domain; zero-, one-, multi-period, loading, and error states remain honest and sample-free.
- Removed Preview semantics from the authoritative Momentum page header and overview while retaining explicit Preview labels on Momentum drivers, Goal Momentum, entry patterns, and suggested guidance that Phase 1 does not calculate.
- Verified the unified headers and authoritative single-period chart in authenticated light/dark browser checkpoints at 1707×960; the authenticated/unauthorized local API smoke checks, 31 Momentum tests, 8 Entries tests, 74 Constellation tests, the known-valid TypeScript check, a 50-route web export, and `git diff --check` pass.
- Added `AuthenticatedPageShell` to align Echo/Entries, full Momentum, and Goals with Home’s uncapped authenticated frame (24px desktop and 16px compact outer gutters) without changing Home’s composition.
- Expanded Echo’s outer page frame, reflection start surface, control row, and entry-group surface to the Home workspace width; preserved existing Notes/Reflection behavior and data loading.
- Expanded the full Momentum Preview workspace and responsive plot area to the Home frame while retaining all current preview semantics, range controls, charts, and calculation/data boundaries.
- Added the canonical target-mark Goals navigation item between Home and Echo, with an active `/goals` route and the same selected, hover, and focus states as the established Sidebar items.
- Corrected the Sidebar account fallback to use the user’s display name or username initial with a high-contrast canonical-green avatar in both themes, including hover and keyboard-focus treatment.
- Consolidated active Home and application-shell branding onto one semantic green family (finalized at `#4A7C5F` light and `#8FAE8A` dark), including the original OHARA mark, selected navigation, Today, Echo, Goals, Momentum, project marks, focus dots, links, and actions; inactive navigation and goal-category colors remain intentionally neutral/semantic.
- Updated `BrandIcon` to use the supported image tint prop, restored the canonical target mark for Goals, removed the duplicate legacy project glyph from the Home project row, and widened the Dashboard workspace by removing its 1760px ceiling while standardizing 24px desktop gutters, 20px frame padding, 24px grid gaps, and the existing 30/34/36 column proportions.
- Visually verified the authenticated populated Home in light/dark mode at 1727×1000 plus 1440×900, 1280×800, 900×900 tablet, and 390×844 mobile checkpoints; authoritative Momentum remained `4.52`, Weekly Streak `3`, Tasks Completed `1`, with no horizontal overflow or database/environment changes.
- Reconstructed the active authenticated Home composition in `app/(app)/dashboard.tsx` around the approved light/dark references: the greeting now anchors the left column above unboxed Today priorities and Echo, authoritative Momentum owns the center feature card, two real activity-ranked Goals occupy the right card, and Projects uses a full-width composed surface.
- Updated `components/layout/Sidebar.tsx` for the Home reference rhythm and functional route order (`Home`, `Echo`, `Momentum`, `Constellation`) while intentionally withholding a Goals item because no functional Goals index route exists yet; updated `features/projects/components/ProjectCard.tsx` to surface real aggregate goal progress without changing project behavior.
- Preserved the frozen Momentum API and Phase 1 calculation boundary while displaying its real current value, weekly change, Weekly Streak, and Tasks Completed This Week in the rebuilt Momentum hierarchy; no Supabase configuration, migration, schema, authentication, route, or calculation code was changed.
- Validated the Home reconstruction with authenticated light/dark browser checkpoints against the restored loopback-only stack, including authoritative Momentum values (`4.52`, Weekly Streak `3`, Tasks Completed `1`), the real one-goal/no-project fixture states, the Momentum expand interaction, the known-valid TypeScript command, 29 Momentum tests, 6 active-goal selector tests, 8 Entries tests, a 50-route Expo web export, and `git diff --check`.
- Completed the isolated local product review after migrations 001–039 and recorded **Ready with documented non-blocking issues**: the migration repair, 35/35 RLS state, embedding indexes, PostgREST data grants, authoritative Home Momentum values, and immutable snapshot chain passed; no source code, migration, remote database, or git history was changed.
- Restored the original documented `vector(1024)` embedding contract in squashed baseline migrations 001, 002, and 004 so the existing HNSW indexes can be created from an empty database.
- Updated the guarded Momentum local fixture to use the profile created by the canonical Auth trigger and to supply the required real goal category when validating the complete repository schema.
- Updated Supabase and Momentum local documentation for the repaired chain and next migration number; Migration 038 calculation and security logic remains unchanged.
- Validated three empty local migration replays through 039 and seed loading, identical final catalog fingerprints across the last two replays, 35/35 public tables with RLS, Momentum's 29 source tests/10 scenarios/5 adversarial assertions, Momentum and Constellation database harnesses, live friendship security, authenticated API/Home data, TypeScript, feature tests, and a 50-route web export.
- Remediated Momentum Phase 1's release blockers: the API now separates authenticated canonical reads from service-role-only publication, Migration 038 revokes client publication/DML and enforces immutable snapshots, and planned-action completion intersects completion IDs with the exact normalized denominator set.
- Updated Momentum diagnostics, Supabase types, migration contracts, local setup, tests, and reports to cover per-action eligibility, trusted hashes/reasons, changed-input revisions, adversarial writes, and real local Home values.
- Validated the remediation with 29 source tests, PostgreSQL 15/16 database harnesses, the actual authenticated API, rendered Home metrics, the known-valid source type-check, and a 50-route web export.
- Connected the active Home Momentum preview to backend-authoritative values and explicit loading/unavailable states, removing its static sample values without changing the full Momentum route or Home layout.
- Made completed-week snapshot publication deterministic and idempotent through stable SHA-256 calculation hashes, immutable superseding revisions, row locking, event deduplication, and stale-baseline rejection.
- Replaced the active Home dashboard composition in `app/(app)/dashboard.tsx`: Today’s Focus now has unboxed priority rows, Momentum uses a substantially larger chart composition, Echo sits beside the daily focus, and Goals shows the two most recently reflective active goals with real milestone/reflection context and next steps.
- Corrected the rendered Constellation field in `features/constellation/components/ConstellationCanvasShell.tsx` by using the theme’s graph-space ambient color and visually verified green ring strokes/opacities while retaining the Current Season coordinate anchor and transformed SVG layer.
- Replaced the remaining Home Goals and Project preview JSX compositions and moved Constellation’s radial field into the transformed graph layer, anchored to Current Season beneath three 8%/14%/20% dashed rings.
- Applied the final system-font typography hierarchy, exact semantic light/dark surface tokens, premium Goal card composition, and fixed dual-theme Constellation depth/ring treatment from the approved visual board.
- Re-composed Home Today’s Focus, Momentum, Echo, Goal, and Project presentation around the target card hierarchy while preserving existing data, routes, and actions.
- Restyled the integrated Entries library and reflection launcher with canonical semantic tokens and shared Card, Button, and Modal primitives.
- Modernized the semantic dark theme and focused reflection palette in `constants/colors.ts`, `constants/focused-tokens.ts`, and `tailwind.config.js` with layered blue-green near-black surfaces and restrained brighter green accents.
- Updated shared UI primitives and layouts across `components/ui/` and `components/layout/` with calmer card hierarchy, semantic elevation, consistent radii, refined controls, generous spacing, glass-like overlays, focus treatment, and improved responsive page rhythm.
- Refined dashboard, project, and goal surfaces in `app/(app)/dashboard.tsx`, `features/projects/components/ProjectCard.tsx`, and `features/goals/components/GoalCard.tsx` to use the modernized hierarchy while preserving all existing actions and workflows.
- Improved global web typography rendering, selection, focus visibility, and scrollbar styling in `global.css`.


---

_Older entries have been rolled into the archive:_
**[`CHANGELOGCODEX-full.md`](https://github.com/jvillalta1903-cmyk/oharaai-archive/blob/main/CHANGELOGCODEX-full.md)**
_(repo: `jvillalta1903-cmyk/oharaai-archive`). Keep this file to a recent rolling
window — when it grows, prepend the aged-out entries into the archived full file._
