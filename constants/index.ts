export const GOAL_CATEGORIES = ['body', 'mind', 'money', 'create', 'connect', 'contribute'] as const;

export const FEATURES = {
  STARLOG_ENABLED: false,        // Pillar 2 — not built yet
  INTELLIGENCE_ENABLED: false,   // Pillar 3 — not built yet
  DISCOVERY_ENABLED: false,      // Pillar 4 — not built yet
  SOCIAL_ENABLED: false,         // Phase 2
  COLLAGE_ENABLED: false,        // Phase 2
} as const;
