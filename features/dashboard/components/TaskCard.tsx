import { View, Text } from 'react-native';

export function TaskCard() {
  return (
    <View className="bg-card-bg rounded-xl p-3">
      <Text className="text-near-black">TaskCard</Text>
    </View>
  );
}
